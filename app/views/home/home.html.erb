
	<% if current_user && current_user.role?(:admin) %> 
		<%= render :partial => 'admin_cards' %>
		<div class="col s6">
		<%= render :partial => 'admin_chart' %>
		</div>
		

	<% elsif (current_user && current_user.role?(:customer))%> 
		<%= render :partial => 'carousel' %>
		<%= render :partial => 'shop_chess' %>

	<% elsif (current_user && current_user.role?(:shipper))%> 
		<%= render :partial => 'partials/shipping_list' %>

	<% elsif (current_user && current_user.role?(:manager))%> 
		<%= render :partial => 'manager_home'%>

	<!-- Be my guest! --> 
	<% else %> 
		<%= render :partial => 'carousel' %>
		<%= render :partial => 'shop_chess' %>

	<% end %>


